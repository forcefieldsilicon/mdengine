# systemd drop-in for the distro caddy.service: its sandbox denies /var/log/caddy even when the
# directory is caddy-owned; declare it so the access log in Caddyfile can be written.
[Service]
LogsDirectory=caddy
ReadWritePaths=/var/log/caddy
